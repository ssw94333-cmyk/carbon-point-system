# 智慧碳积分管理系统功能设计

# 数据库设计

## 用户表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| username | VARCHAR | 50 | 用户名，唯一 |
| password | VARCHAR | 255 | 密码，加密存储 |
| phone | VARCHAR | 20 | 手机号，唯一 |
| nickname | VARCHAR | 50 | 昵称 |
| avatar | VARCHAR | 255 | 头像URL |
| email | VARCHAR | 100 | 邮箱 |
| user_type | INTEGER | - | 用户类型，0-普通用户，1-管理员 |
| status | INTEGER | - | 状态，0-禁用，1-启用 |
| total_points | INTEGER | - | 累计积分总额 |
| current_points | INTEGER | - | 当前可用积分 |
| total_carbon_reduction | DECIMAL | 10,2 | 累计减碳量，单位：千克 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 碳行为记录表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| user_id | BIGINT | - | 用户ID，外键关联用户表 |
| behavior_type | VARCHAR | 50 | 行为类型，如步行、骑行、公交出行等 |
| behavior_name | VARCHAR | 100 | 行为名称 |
| behavior_value | DECIMAL | 10,2 | 行为数值，如步行公里数、骑行公里数等 |
| carbon_reduction | DECIMAL | 10,2 | 减碳量，单位：千克 |
| points | INTEGER | - | 获得积分 |
| description | TEXT | - | 行为描述 |
| proof_image | VARCHAR | 500 | 证明材料图片URL，多个用逗号分隔 |
| audit_status | INTEGER | - | 审核状态，0-待审核，1-审核通过，2-审核拒绝 |
| audit_user_id | BIGINT | - | 审核人ID，外键关联用户表 |
| audit_time | DATETIME | - | 审核时间 |
| audit_remark | VARCHAR | 500 | 审核备注 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 行为规则表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| behavior_type | VARCHAR | 50 | 行为类型，唯一 |
| behavior_name | VARCHAR | 100 | 行为名称 |
| unit | VARCHAR | 20 | 单位，如公里、次、度等 |
| carbon_reduction_per_unit | DECIMAL | 10,4 | 每单位减碳量，单位：千克 |
| points_per_unit | INTEGER | - | 每单位获得积分 |
| min_value | DECIMAL | 10,2 | 最小数值限制 |
| max_value | DECIMAL | 10,2 | 每日最大数值限制 |
| max_points_per_day | INTEGER | - | 每日最大积分限制 |
| need_proof | INTEGER | - | 是否需要证明材料，0-不需要，1-需要 |
| status | INTEGER | - | 状态，0-禁用，1-启用 |
| sort_order | INTEGER | - | 排序顺序 |
| description | TEXT | - | 规则说明 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 积分变动记录表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| user_id | BIGINT | - | 用户ID，外键关联用户表 |
| change_type | INTEGER | - | 变动类型，1-获得，2-消费 |
| source_type | VARCHAR | 50 | 来源类型，如碳行为、积分兑换、活动奖励等 |
| source_id | BIGINT | - | 来源ID，关联具体业务表 |
| points | INTEGER | - | 积分变动数量，正数为获得，负数为消费 |
| balance_before | INTEGER | - | 变动前积分余额 |
| balance_after | INTEGER | - | 变动后积分余额 |
| remark | VARCHAR | 500 | 备注说明 |
| create_time | DATETIME | - | 创建时间 |

## 商品表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| name | VARCHAR | 200 | 商品名称 |
| category | VARCHAR | 50 | 商品分类，如实物商品、优惠券、公益捐赠等 |
| description | TEXT | - | 商品描述 |
| image | VARCHAR | 500 | 商品图片URL，多个用逗号分隔 |
| points_required | INTEGER | - | 所需积分 |
| stock | INTEGER | - | 库存数量 |
| total_stock | INTEGER | - | 总库存数量 |
| exchange_limit | INTEGER | - | 每人限兑数量，0表示不限制 |
| status | INTEGER | - | 状态，0-下架，1-上架 |
| sort_order | INTEGER | - | 排序顺序 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 兑换订单表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| order_no | VARCHAR | 50 | 订单号，唯一 |
| user_id | BIGINT | - | 用户ID，外键关联用户表 |
| product_id | BIGINT | - | 商品ID，外键关联商品表 |
| product_name | VARCHAR | 200 | 商品名称，冗余字段 |
| quantity | INTEGER | - | 兑换数量 |
| points_required | INTEGER | - | 所需积分 |
| total_points | INTEGER | - | 总积分 |
| order_status | INTEGER | - | 订单状态，0-待处理，1-已发货，2-已完成，3-已取消 |
| receiver_name | VARCHAR | 50 | 收货人姓名，可空 |
| receiver_phone | VARCHAR | 20 | 收货人电话，可空 |
| receiver_address | VARCHAR | 500 | 收货地址，可空 |
| express_company | VARCHAR | 50 | 快递公司 |
| express_no | VARCHAR | 50 | 快递单号 |
| remark | VARCHAR | 500 | 备注 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 活动表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| title | VARCHAR | 200 | 活动标题 |
| description | TEXT | - | 活动描述 |
| image | VARCHAR | 500 | 活动图片URL，多个用逗号分隔 |
| start_time | DATETIME | - | 开始时间 |
| end_time | DATETIME | - | 结束时间 |
| registration_start | DATETIME | - | 报名开始时间 |
| registration_end | DATETIME | - | 报名结束时间 |
| max_participants | INTEGER | - | 最大参与人数，0表示不限制 |
| current_participants | INTEGER | - | 当前参与人数 |
| reward_points | INTEGER | - | 奖励积分 |
| reward_description | VARCHAR | 500 | 奖励说明 |
| rules | TEXT | - | 参与规则 |
| status | INTEGER | - | 状态，0-未开始，1-进行中，2-已结束，3-已取消 |
| sort_order | INTEGER | - | 排序顺序 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 活动报名表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| activity_id | BIGINT | - | 活动ID，外键关联活动表 |
| user_id | BIGINT | - | 用户ID，外键关联用户表，activity_id和user_id组合唯一 |
| participation_status | INTEGER | - | 参与状态，0-已报名，1-已完成，2-已取消 |
| reward_status | INTEGER | - | 奖励状态，0-未发放，1-已发放 |
| points_rewarded | INTEGER | - | 已奖励积分 |
| participation_record | TEXT | - | 参与记录 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

## 收货地址表

| 字段 | 类型 | 大小 | 说明 |
|------|------|------|------|
| id | BIGINT | - | 主键，自增 |
| user_id | BIGINT | - | 用户ID，外键关联用户表 |
| receiver_name | VARCHAR | 50 | 收货人姓名 |
| receiver_phone | VARCHAR | 20 | 收货人电话 |
| province | VARCHAR | 50 | 省份 |
| city | VARCHAR | 50 | 城市 |
| district | VARCHAR | 50 | 区县 |
| detail_address | VARCHAR | 500 | 详细地址 |
| is_default | INTEGER | - | 是否默认地址，0-否，1-是 |
| create_time | DATETIME | - | 创建时间 |
| update_time | DATETIME | - | 更新时间 |

# 用户模块

## API设计

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/user/register | POST | 用户注册 |
| /api/user/login | POST | 用户登录 |
| /api/user/logout | POST | 用户登出 |
| /api/user/info | GET | 获取当前用户信息 |
| /api/user/info | PUT | 更新用户信息 |
| /api/user/password | PUT | 修改密码 |
| /api/user/points | GET | 获取用户积分信息 |
| /api/user/points/history | GET | 获取积分变动历史记录 |
| /api/user/address | GET | 获取收货地址列表 |
| /api/user/address | POST | 添加收货地址 |
| /api/user/address/{id} | PUT | 更新收货地址 |
| /api/user/address/{id} | DELETE | 删除收货地址 |
| /api/user/address/{id}/default | PUT | 设置默认地址 |
| /api/upload/avatar | POST | 上传用户头像 |

## 后端设计

1. 用户注册功能：接收用户注册请求，验证用户名和手机号唯一性，对密码进行加密存储，初始化用户积分和减碳量为0，创建用户记录并返回注册结果
2. 用户登录功能：验证用户名和密码，验证用户状态是否启用，生成JWT令牌，记录登录日志，返回令牌和用户基本信息
3. 用户登出功能：使当前令牌失效，清除用户会话信息
4. 获取用户信息功能：根据令牌获取用户ID，查询用户详细信息并返回
5. 更新用户信息功能：验证用户身份，更新用户昵称、头像、联系方式等信息，记录更新时间
6. 修改密码功能：验证原密码正确性，对新密码进行加密，更新用户密码
7. 获取积分信息功能：查询用户当前积分余额、累计积分总额和累计减碳量
8. 获取积分变动历史功能：分页查询用户积分变动记录，支持按变动类型和时间范围筛选
9. 获取收货地址列表功能：查询当前用户的所有收货地址，按默认地址和创建时间排序
10. 添加收货地址功能：创建新的收货地址，如果设置为默认地址则取消其他地址的默认状态
11. 更新收货地址功能：修改收货地址信息，支持更新默认地址状态
12. 删除收货地址功能：删除指定的收货地址，如果删除的是默认地址则自动设置第一个地址为默认
13. 设置默认地址功能：将指定地址设置为默认地址，同时取消其他地址的默认状态
14. 上传用户头像功能：接收图片文件，验证文件格式和大小，保存到服务器并返回图片URL

## 管理端功能设计

1. 管理员登录：管理员使用账号密码登录管理后台，验证用户类型为管理员，生成管理端令牌
2. 用户列表管理：分页查询所有用户信息，支持按用户名、手机号、用户类型、状态等条件筛选，显示用户基本信息和积分情况
3. 用户详情查看：查看指定用户的详细信息，包括个人信息、积分变动记录、行为记录、兑换记录等
4. 用户状态管理：启用或禁用用户账号，禁用后用户无法登录系统
5. 用户积分管理：管理员可以手动调整用户积分，记录调整原因和操作人
6. 用户信息编辑：编辑用户基本信息，包括昵称、联系方式等
7. 用户数据统计：统计用户总数、活跃用户数、新增用户数等数据

## 用户端功能设计

1. 用户注册页面：提供注册表单，包含用户名、手机号、密码、确认密码等字段，进行前端验证后提交注册请求
2. 用户登录页面：提供登录表单，包含用户名和密码字段，支持记住密码功能，登录成功后跳转到首页
3. 个人信息页面：展示用户头像、昵称、联系方式等信息，支持修改个人信息和上传头像
4. 修改密码页面：提供修改密码表单，包含原密码、新密码、确认新密码字段，验证后提交修改请求
5. 我的积分页面：展示当前可用积分、累计积分总额、累计减碳量，以图表形式展示积分变动趋势
6. 积分历史记录页面：分页展示积分变动历史记录，包括获得和消费记录，显示变动时间、原因、数量等信息
7. 收货地址管理页面：展示用户的所有收货地址，支持添加、编辑、删除地址，设置默认地址

# 碳行为模块

## API设计

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/behavior/rules | GET | 获取行为规则列表 |
| /api/behavior/rules/{id} | GET | 获取行为规则详情 |
| /api/behavior/record | POST | 提交行为记录 |
| /api/behavior/record | GET | 获取行为记录列表 |
| /api/behavior/record/{id} | GET | 获取行为记录详情 |
| /api/behavior/record/{id} | DELETE | 删除行为记录 |
| /api/behavior/statistics | GET | 获取行为统计数据 |
| /api/upload/behavior-proof | POST | 上传行为证明材料 |
| /api/behavior/audit/list | GET | 获取待审核行为记录列表 |
| /api/behavior/audit/{id} | PUT | 审核行为记录 |
| /api/behavior/rules | POST | 创建行为规则 |
| /api/behavior/rules/{id} | PUT | 更新行为规则 |
| /api/behavior/rules/{id} | DELETE | 删除行为规则 |

## 后端设计

1. 获取行为规则列表功能：查询所有启用的行为规则，按排序顺序返回，供用户查看各类行为的积分规则
2. 获取行为规则详情功能：根据规则ID查询规则详细信息，包括单位、积分计算方式、限制条件等
3. 提交行为记录功能：接收用户提交的行为记录，验证行为类型和数值是否符合规则，计算减碳量和积分，保存记录并设置为待审核状态
4. 获取行为记录列表功能：分页查询当前用户的行为记录，支持按行为类型、审核状态、时间范围筛选
5. 获取行为记录详情功能：根据记录ID查询行为记录详细信息，包括行为内容、证明材料、审核状态等
6. 删除行为记录功能：允许用户删除待审核状态的行为记录，已审核的记录不允许删除
7. 获取行为统计数据功能：统计用户各类行为的次数、总减碳量、总获得积分等数据，支持按时间范围统计
8. 获取待审核行为记录列表功能：管理员查询所有待审核的行为记录，支持按用户、行为类型、时间范围筛选
9. 审核行为记录功能：管理员审核行为记录，审核通过时发放积分并更新用户积分和减碳量，审核拒绝时记录拒绝原因
10. 创建行为规则功能：管理员创建新的行为规则，设置行为类型、积分计算方式、限制条件等
11. 更新行为规则功能：管理员修改现有行为规则，更新规则参数和状态
12. 删除行为规则功能：管理员删除行为规则，已使用的规则不允许删除
13. 上传行为证明材料功能：接收图片文件，验证文件格式和大小，保存到服务器并返回图片URL

## 管理端功能设计

1. 行为规则管理：列表展示所有行为规则，支持新增、编辑、删除规则，设置规则参数和状态
2. 行为记录审核：列表展示待审核的行为记录，显示用户信息、行为内容、证明材料等，支持审核通过或拒绝操作
3. 行为记录查询：查询所有用户的行为记录，支持按用户、行为类型、审核状态、时间范围等条件筛选
4. 行为数据统计：统计平台各类行为的提交次数、审核通过率、发放积分总量等数据
5. 行为规则配置：配置行为规则的积分计算方式、限制条件、是否需要证明材料等参数

## 用户端功能设计

1. 行为规则查看页面：展示所有可用的行为规则，包括行为名称、单位、积分计算方式、限制条件等说明
2. 行为记录提交页面：提供表单供用户提交行为记录，包括选择行为类型、输入行为数值、上传证明材料等，自动计算减碳量和积分
3. 我的行为记录页面：分页展示用户提交的所有行为记录，显示行为类型、数值、减碳量、积分、审核状态等信息
4. 行为记录详情页面：查看单条行为记录的详细信息，包括提交内容、证明材料、审核结果等
5. 行为统计页面：以图表形式展示用户各类行为的统计情况，包括行为次数、减碳量、获得积分等数据
6. 行为记录删除功能：允许用户删除待审核状态的行为记录

# 积分商城模块

## API设计

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/product/list | GET | 获取商品列表 |
| /api/product/{id} | GET | 获取商品详情 |
| /api/product | POST | 创建商品 |
| /api/product/{id} | PUT | 更新商品 |
| /api/product/{id} | DELETE | 删除商品 |
| /api/product/{id}/status | PUT | 上架或下架商品 |
| /api/upload/product-image | POST | 上传商品图片 |
| /api/order/exchange | POST | 积分兑换商品 |
| /api/order/list | GET | 获取兑换订单列表 |
| /api/order/{id} | GET | 获取订单详情 |
| /api/order/{id}/cancel | PUT | 取消订单 |
| /api/order/{id}/ship | PUT | 订单发货 |
| /api/order/{id}/complete | PUT | 完成订单 |

## 后端设计

1. 获取商品列表功能：分页查询上架状态的商品，支持按分类、积分范围、关键词搜索筛选，按排序顺序返回
2. 获取商品详情功能：根据商品ID查询商品详细信息，包括商品描述、图片、所需积分、库存等
3. 创建商品功能：管理员创建新商品，设置商品名称、分类、描述、图片、所需积分、库存等信息
4. 更新商品功能：管理员修改商品信息，更新商品参数和状态
5. 删除商品功能：管理员删除商品，已有兑换记录的商品不允许删除
6. 上架或下架商品功能：管理员修改商品上架状态，下架商品不再显示在商品列表中
7. 积分兑换商品功能：验证用户积分是否充足，验证商品库存和限兑数量，扣除用户积分，创建兑换订单，减少商品库存，记录积分变动
8. 获取兑换订单列表功能：分页查询用户的兑换订单，支持按订单状态、时间范围筛选
9. 获取订单详情功能：根据订单ID查询订单详细信息，包括商品信息、收货信息、订单状态等
10. 取消订单功能：允许用户取消待处理状态的订单，退还积分并恢复商品库存
11. 订单发货功能：管理员更新订单发货信息，包括快递公司和快递单号，更新订单状态为已发货
12. 完成订单功能：用户确认收货后，更新订单状态为已完成
13. 上传商品图片功能：接收图片文件，验证文件格式和大小，保存到服务器并返回图片URL

## 管理端功能设计

1. 商品管理：列表展示所有商品，支持新增、编辑、删除商品，设置商品信息、图片、积分、库存等
2. 商品上架管理：控制商品的上架和下架状态，下架商品不对外展示
3. 兑换订单管理：列表展示所有兑换订单，显示订单信息、用户信息、商品信息、订单状态等
4. 订单发货管理：处理订单发货，填写快递公司和快递单号，更新订单状态
5. 订单查询统计：查询订单数据，统计兑换数量、积分消费总量、热门商品等数据
6. 库存管理：查看商品库存情况，及时补充库存不足的商品

## 用户端功能设计

1. 商品列表页面：分页展示可兑换的商品，支持按分类筛选和关键词搜索，显示商品图片、名称、所需积分等信息
2. 商品详情页面：展示商品详细信息，包括商品描述、多张图片、所需积分、库存数量、兑换说明等
3. 积分兑换页面：用户选择商品和数量，选择或填写收货地址，确认兑换信息，验证积分和库存，提交兑换请求
4. 我的订单页面：分页展示用户的兑换订单列表，显示订单号、商品信息、所需积分、订单状态、创建时间等
5. 订单详情页面：查看订单详细信息，包括商品信息、收货信息、订单状态、物流信息等
6. 订单取消功能：用户可以在订单待处理状态下取消订单，系统自动退还积分
7. 订单确认收货功能：用户收到商品后可以确认收货，完成订单流程

# 活动模块

## API设计

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/activity/list | GET | 获取活动列表 |
| /api/activity/{id} | GET | 获取活动详情 |
| /api/activity/register | POST | 报名参加活动 |
| /api/activity/my | GET | 获取我的活动列表 |
| /api/activity/{id}/cancel | PUT | 取消活动报名 |
| /api/activity | POST | 创建活动 |
| /api/activity/{id} | PUT | 更新活动 |
| /api/activity/{id} | DELETE | 删除活动 |
| /api/activity/{id}/participants | GET | 获取活动参与人员列表 |
| /api/activity/{id}/reward | POST | 发放活动奖励 |
| /api/upload/activity-image | POST | 上传活动图片 |

## 后端设计

1. 获取活动列表功能：分页查询活动，支持按状态筛选，返回进行中和即将开始的活动，按开始时间排序
2. 获取活动详情功能：根据活动ID查询活动详细信息，包括活动描述、规则、奖励、参与人数等
3. 报名参加活动功能：验证活动是否在报名时间内，验证参与人数是否已满，创建报名记录，更新活动参与人数
4. 获取我的活动列表功能：查询当前用户报名的所有活动，支持按活动状态筛选
5. 取消活动报名功能：允许用户在活动开始前取消报名，更新报名状态和活动参与人数
6. 创建活动功能：管理员创建新活动，设置活动标题、描述、时间、规则、奖励等信息
7. 更新活动功能：管理员修改活动信息，更新活动参数和状态
8. 删除活动功能：管理员删除活动，已有报名记录的活动不允许删除
9. 获取活动参与人员列表功能：管理员查询活动的所有参与人员，显示用户信息和参与状态
10. 发放活动奖励功能：管理员为完成活动的用户发放积分奖励，更新用户积分和奖励状态
11. 上传活动图片功能：接收图片文件，验证文件格式和大小，保存到服务器并返回图片URL

## 管理端功能设计

1. 活动管理：列表展示所有活动，支持新增、编辑、删除活动，设置活动信息、时间、规则、奖励等
2. 活动状态管理：查看活动状态，包括未开始、进行中、已结束等，系统自动更新活动状态
3. 活动参与人员管理：查看活动的参与人员列表，显示用户信息和参与状态
4. 活动奖励发放：为完成活动的用户发放积分奖励，批量或单个发放奖励
5. 活动数据统计：统计活动参与人数、完成人数、发放奖励总量等数据

## 用户端功能设计

1. 活动列表页面：展示正在进行和即将开始的活动，显示活动标题、图片、时间、参与人数等信息
2. 活动详情页面：展示活动详细信息，包括活动描述、参与规则、奖励机制、参与人数等，提供报名按钮
3. 活动报名页面：用户查看活动详情后可以报名参加，系统验证报名条件后创建报名记录
4. 我的活动页面：展示用户报名的所有活动，显示活动信息、参与状态、奖励状态等
5. 活动取消报名功能：用户可以在活动开始前取消报名，释放参与名额
6. 活动参与记录查看：查看已参与活动的详细记录和获得的奖励信息

# 数据统计模块

## API设计

| 端点 | 方法 | 描述 |
|------|------|------|
| /api/statistics/dashboard | GET | 获取首页数据统计 |
| /api/statistics/carbon-footprint | GET | 获取个人碳足迹数据 |
| /api/statistics/ranking | GET | 获取排行榜数据 |
| /api/statistics/report | GET | 获取数据报表 |
| /api/statistics/user-growth | GET | 获取用户增长数据 |
| /api/statistics/behavior-distribution | GET | 获取行为分布数据 |
| /api/statistics/exchange-statistics | GET | 获取兑换统计数据 |

## 后端设计

1. 获取首页数据统计功能：统计平台总体数据，包括用户总数、活跃用户数、总减碳量、总发放积分、总消费积分等关键指标
2. 获取个人碳足迹数据功能：统计用户个人的减碳趋势，支持按日、周、月、年统计，返回减碳量数据和图表数据
3. 获取排行榜数据功能：查询积分排行榜和减碳排行榜，支持按总积分、当前积分、总减碳量等维度排序，返回前N名用户信息
4. 获取数据报表功能：管理员查询平台运营数据报表，包括用户增长趋势、行为分布、兑换统计等综合数据
5. 获取用户增长数据功能：统计用户注册增长趋势，支持按日、周、月统计新增用户数
6. 获取行为分布数据功能：统计各类行为的提交次数、审核通过次数、发放积分等分布情况
7. 获取兑换统计数据功能：统计商品兑换数量、积分消费总量、热门商品等兑换相关数据

## 管理端功能设计

1. 首页数据展示：以仪表盘形式展示平台关键指标，包括用户数量、总减碳量、活跃度、积分发放和消费情况等
2. 用户增长报表：以图表形式展示用户注册增长趋势，支持按时间维度查看
3. 行为分布报表：统计各类行为的分布情况，以图表形式展示行为提交和审核通过情况
4. 兑换统计报表：统计商品兑换数据，包括兑换数量、积分消费、热门商品等
5. 数据导出功能：支持将统计数据导出为Excel文件，方便管理员进行数据分析
6. 综合数据报表：整合各类统计数据，生成综合运营报表，支持按时间范围查询

## 用户端功能设计

1. 首页数据展示：展示平台总体数据概览，包括用户总数、总减碳量等关键指标，激励用户参与
2. 个人碳足迹页面：以图表形式展示用户个人的减碳趋势，支持查看日、周、月、年等不同维度的统计数据
3. 积分排行榜页面：展示积分排行榜和减碳排行榜，显示排名、用户名、积分、减碳量等信息，用户可以查看自己的排名位置
4. 减碳排行榜页面：展示减碳量排行榜，显示排名、用户名、累计减碳量等信息，激励用户参与低碳行为
5. 个人数据统计页面：综合展示用户的各项数据，包括行为统计、积分变动、兑换记录等

